<!-- note-audit: note=86 -->
<!--
  🔴 C-109 · CE MARQUEUR EST LU PAR `npm run check:docs-scored`.
  Douze documents de fond n'étaient notés par RIEN : ils ne pouvaient ni monter
  ni baisser, donc **rien ne signalait qu'ils avaient vieilli**.
  Chaque document de `docs/` déclare donc soit sa note, soit la note qui le
  couvre, soit qu'il n'est pas noté ET pourquoi.
  ❌ Ne JAMAIS inventer une note sans avoir audité le domaine : `non-note` est
     une réponse honnête, un chiffre faux ne l'est pas.
-->
> **Note d'audit** — Note **86 / 100**, au tableau de bord de [`README.md`](./README.md).

# i18n — qualité des traductions et périmètre réel

## Note i18n : 90 → **86 / 100** (2026-09-14, première note attribuée · puis 2026-09-16) · **VÉRIFIÉE inchangée le 2026-09-14 au soir**

> ### 🟠 2026-09-16 · -4 : la note comptait ce qui était mesuré, jamais ce qui ne l'était pas
>
> **Ce n'est pas une régression.** Rien n'a cassé depuis la dernière passe. Les angles morts
> listés juste en dessous **existaient tous** pendant que cette note montait : elle était
> surévaluée parce qu'elle ne comptait que ce que les gardes regardent. C'est exactement ce qui
> s'est produit le 2026-09-14, où cinq notes ont baissé sans qu'aucun défaut ne soit récent.
>
> **Barème, déclaré pour être contestable ligne par ligne :**
>
> | Situation | Effet |
> |---|---|
> | angle mort **structurel**, de portée large, qu'aucun outil ne regarde | −2 |
> | angle mort réel mais de portée limitée, ou partiellement couvert | −1 |
> | angle mort **assumé** (arbitrage documenté), ou déjà compté dans une passe antérieure | 0 |
> | angle mort **comblé** le jour même, avec garde **et** témoin | +1 |
>
> **Le calcul pour cette note :**
>
> | Angle mort | Effet | Pourquoi |
> |---|---|---|
> | AM-1 · **les trois gates mesurent les clés et les copies, jamais la QUALITÉ** | −2 | une traduction fausse mais différente passe les trois sans bruit |
> | AM-2 · le CORPS des pages `en` n'est comparé à rien depuis `C-20` | −1 | mesuré une fois avant l'ouverture à l'indexation |
> | AM-3 · pluriels et formats de date sans garde dédiée | −1 | `tp()` et `format.ts` |
> | AM-4 · `es` déclarée mais ni servie ni indexable | 0 | mécanisme prévu, couvert par AM-2 |
>
> 🔴 **Une note baisse UNE FOIS, quand l'angle mort est nommé ; elle remonte quand il est
> outillé.** Sans cette règle, nommer un angle mort deviendrait punitif, et la passe du
> 2026-09-16 serait la dernière à en chercher. Un angle mort reconduit sans être comblé ne
> re-coûte rien : il est **déjà payé**.
>
> ⚠️ Un transversal (T-1 à T-10 du [tableau de bord](./README.md)) est compté dans **chaque** audit
> qu'il touche, parce que chaque note prétend quelque chose de différent. Les 36 témoins jamais
> rejoués coûtent donc à la fois aux tests et à la sécurité, et ce n'est pas un double comptage.



### 🕳️ Angles morts · ce que cet audit NE mesure PAS (2026-09-16)

> ## ✅ Relu le 2026-09-21 · **une ligne de ce tableau est désormais OUTILLÉE**
>
> La passe du 2026-09-20 au soir (`2b4c4304`) a traité les 30 items du § 12
> d'[`a-faire-code.md`](../a-faire-code.md), qui sont nés de ces angles morts.
>
> ❌ **Le tableau ci-dessous n'est PAS réécrit.** Il décrit le 2026-09-16, et c'est sa date qui
> lui donne sa valeur : un angle mort nommé est ce qui a permis de l'outiller. Ce bandeau dit ce
> qui le couvre aujourd'hui, et surtout **ce que la garde ne prouve pas** — la moitié qui manque
> d'habitude.
>
> | Angle mort | Couvert par | 🔴 Ce que ça ne prouve PAS |
> |---|---|---|
> | AM-2 · AM-3 · AM-4 | `npm run i18n:pages` (`C-99`) : corps des pages prérendues par locale, pluriels, formats | La **qualité** des traductions, qui reste un geste (`M-50`). 🔴 Première exécution : **23 formes plurielles mortes** en `en`, `_many` recopié du français |
>
> ⚠️ **Les lignes du tableau qui ne sont pas citées ici restent OUVERTES**, et une garde posée
> n'est pas un angle mort fermé : `M-56` (« ce que chaque angle mort coûte en points ») n'est
> toujours pas tranché, donc aucune note ne bouge sur cette base.


> **Pourquoi cette section existe.** Cette note est justifiée par des points **nommés** (« ce qui
> retient à N », suivi d'une liste). Une note construite ainsi ne peut baisser que sur un défaut
> que quelqu'un a d'abord nommé : **un angle mort ne pèse rien tant qu'il reste anonyme**, et ce
> n'est pas un oubli d'auditeur, c'est une propriété de la méthode de notation.
>
> Le prototype du problème est daté : `CLAUDE.md` a pesé 150 ko et ~43 000 tokens sans qu'aucune
> note ne bouge, alors qu'il est cité **13 fois** dans [`ARCHITECTURE.md`](./ARCHITECTURE.md), dont **9**
> dans la seule colonne « Où il est écrit » du tableau des invariants, et **jamais** dans ce
> qui est mesuré. Il était le mètre,
> jamais l'objet.
>
> Ces lignes entrent donc **dans ce qui est mesuré**. La prochaine passe les traite comme les
> invariants ci-dessus : chacune est soit comblée, soit reconduite avec sa date.

| # | Angle mort | Vérifié le 2026-09-16 | Outillable ? |
|---|---|---|---|
| AM-1 | 🔴 **Les trois gates mesurent les CLÉS et les COPIES, jamais la QUALITÉ.** `i18n:check` compare des clés, `i18n:identical` détecte un `en` recopié du `fr`. Une traduction **fausse mais différente** passe les trois sans bruit | les trois gates sont vertes avec 3 898 couples | non par un script : seule une relecture humaine datée le comble |
| AM-2 | **Le CORPS des pages prérendues en `en` n'est comparé à rien.** Les 50 pages ont été mesurées une fois avant l'ouverture d'`en` à l'indexation (`C-20`), jamais depuis | aucune garde ne rejoue cette comparaison | oui : une garde de volume de texte par page et par locale |
| AM-3 | **Les pluriels et les formats de date ne sont testés que ponctuellement.** `tp()` et `src/i18n/format.ts` n'ont pas de garde dédiée balayant les deux locales | aucun test de parité de pluriels | oui |
| AM-4 | **`es` figure dans `route-slugs.json` sans être servie ni indexable.** C'est le mécanisme prévu, mais rien ne garde l'écart : un ajout à `SUPPORTED_LOCALES` sans traduction du corps ne fait échouer aucun job | `i18n:check` garde l'état à moitié ouvert, pas la qualité du corps | partiellement (AM-2 y répond) |


> ### ⚪ 2026-09-14 (soir) · 0 : les trois gates rejouées rendent les mêmes chiffres, à l'unité
>
> `i18n:check` **23 namespaces, 0 erreur, 0 avertissement** · `i18n:scan` **0 chaîne** ·
> `i18n:identical` **3 898 couples, 92 identiques, 92 déclarées, 0 non déclarée**. Identique au
> matin, ce qui est le résultat attendu d'une journée sans travail de traduction, et qui n'avait
> aucune raison d'être supposé.
>
> ⚠️ **Deux fichiers annonçaient encore « 19 namespaces », et ce document avait raison contre eux**
> : `CLAUDE.md` § i18n et `PERFORMANCE.md` § catalogue. Le chiffre datait du levier de chargement
> paresseux du 2026-08-25 ; quatre catalogues sont entrés depuis. Corrigés ce soir, ainsi que le
> commentaire de `src/i18n/catalog.ts` qui parlait des « 17 autres » namespaces paresseux, désormais
> **21**. Poids mesuré sur disque au passage : **244 ko** de JSON `fr`, 215 ko de `en`, contre les
> 178 ko cités comme état d'avant-levier.
>
> 🔴 **Angle mort · les articles de blog anglais portent des slugs FRANÇAIS.** Mesuré sur le build
> de ce soir : `/en/blog/combien-de-temps-prendre-habitude`,
> `/en/blog/gestion-du-temps-efficace`, `/en/blog/methode-okr-exemples`, et ainsi pour les onze.
> **Ce n'est pas un oubli** : `src/content/blog/*.mjs` pose la règle en toutes lettres, « la colonne
> vertébrale (`slug`, dates, `related`) est COMMUNE à toutes les langues : c'est une seule
> publication traduite, pas deux articles ». L'arbitrage se défend, il évite deux identités pour un
> même contenu.
>
> Ce qui manquait, c'est qu'il ne soit écrit **nulle part dans un audit** : ni ici, ni dans
> [`SEO.md`](./SEO.md). Or il a un prix, et il tombe sur le marché qu'on ouvre : un slug est un
> signal de pertinence, et onze articles anglais concourent avec une URL que leur lecteur ne peut
> pas lire. La décision reste la bonne tant que l'anglais n'a aucun trafic ; **elle doit être
> rouverte le jour où l'anglais en a**, et ce jour-là le coût d'un changement de slug est une table
> de redirections 301, pas une réécriture. Consigné pour que la question soit posée plutôt que
> redécouverte.
>
> ⚠️ Rappel de ce que cette note ne mesure toujours pas : la **qualité** des 26 pages anglaises.
> Aucune relecture par un locuteur natif n'a eu lieu, et aucune gate ne peut y suppléer.


**Audit du 2026-08-14** — premier audit de ce domaine. Mesuré sur les catalogues de
`src/locales/` et le contenu éditorial de `src/content/`.

> ### ⚠️ Note du 2026-09-03 · les chiffres du §1 datent du 2026-08-14 et n'ont PAS été remesurés
>
> Trois choses ont bougé depuis, aucune ne change les mesures ci-dessous, et la troisième explique
> un chiffre qui a l'air d'une régression sans en être une :
>
> - **`npm run i18n:scan` est une gate CI bloquante depuis le 2026-09-02.** Il compte les chaînes
>   d'interface **en dur**, celles qui ne sont dans aucun catalogue : un axe que cet audit ne
>   mesurait pas du tout ;
> - **le seuil est passé de 4 à 25, sans qu'une seule chaîne ait été ajoutée.** Le seuil valait 4
>   sur une heuristique **aveugle à quatre formes entières** : texte JSX contenant une
>   interpolation, texte sur plusieurs lignes, propriété d'objet `label:` (par opposition à
>   l'attribut `label=`), et valeur par défaut de prop. La gate certifiait « plus aucune chaîne
>   d'interface en dur » pendant qu'il en restait des dizaines dans le produit. **C'est la MESURE
>   qui a changé, pas la dette**, et `src/components` est à zéro depuis. `-- --list` dit
>   lesquelles ;
> - **les trois pages contractuelles (CGU, confidentialité, mentions légales) existent en
>   anglais**, avec une clause de langue disant que **le français prévaut**. Modifier leur fond
>   n'est donc pas une tâche de traduction, c'est modifier un contrat.
>
> 🟢 **PREMIÈRE NOTE ATTRIBUÉE le 2026-09-14 : 90 / 100.** Aucune n'avait jamais été posée sur ce
> document — le périmètre réellement bilingue n'était pas remesuré écran par écran depuis le
> 2026-08-14, et les trois gates n'existaient pas encore pour la plupart des dates citées ici. Ce
> n'est plus le cas : les trois tournent, sur le catalogue d'AUJOURD'HUI, pas sur celui du 14 août.
>
> | Gate | Résultat le 2026-09-14 |
> |---|---|
> | `npm run i18n:check` | ✅ 23 namespaces, **0** erreur, **0** avertissement — parité des clés |
> | `npm run i18n:scan` | ✅ **0** chaîne d'interface en dur (cliquet CI à 0) |
> | `npm run i18n:identical` | ✅ **3 898** couples comparables (+90 depuis le 09-08, sous-catégories arrivées), **92** identiques, **92** déclarées légitimes, **0** non déclarée |
> | Contenu éditorial | ✅ **11/11** articles de blog, **4/4** pages cas d'usage portent un corps `en` (vérifié dans `src/content/`, pas déduit du prérendu) |
>
> **Ce qui retient la note à 90, et pas plus haut** : aucun gate ne peut mesurer la QUALITÉ d'une
> traduction, seulement sa présence et sa non-identité au français. C'est la même limite que
> l'accessibilité pose entre « focus » et « annonce » (`docs/ACCESSIBILITY.md`) — une lecture
> humaine, par un locuteur natif, des 26 pages anglaises n'a jamais été faite. Et c'est une
> **première** note : elle part prudente plutôt qu'au plafond, un domaine qu'on note pour la
> première fois n'a pas l'historique qui justifierait un 95.

Le fonctionnement du socle est décrit dans [`../CLAUDE.md`](../CLAUDE.md) §i18n ; l'indexation par
locale dans [`SEO.md`](./SEO.md). Ce document ne traite que d'une question : **le produit est-il
réellement bilingue ?**

---

## 1. Les catalogues d'interface sont réellement traduits

Méthode : comparer chaque valeur `en` à sa valeur `fr`. Une valeur identique signale une clé non
traduite, aux noms propres, sigles et symboles près. Mécanisée depuis le 2026-09-08 par
`npm run i18n:identical`.

| Mesure (2026-09-08) | Résultat |
|---|---|
| Clés dans le catalogue de référence `fr` | **3 879** sur 23 namespaces |
| Couples de clés comparables (présentes dans `fr` ET `en`) | **3 808** |
| Valeurs `en` identiques au `fr` | **92 (2,4 %)** |
| dont **déclarées légitimes** (noms propres, sigles, symboles, anglicismes) | **92** |
| dont **non traduites** | **0** |
| `npm run i18n:check` | ✅ 0 erreur, 0 avertissement, parité des clés respectée |
| `npm run i18n:identical` | ✅ cliquet à 0 |

⚠️ **Le chiffre de « 71 (3 %) sur 2 476 clés » qui figurait ici datait du 2026-08-14 et n'avait
jamais été remesuré**, alors que le catalogue a gagné 1 403 clés depuis. Il servait d'appui à
l'item C-21 : le tri légitimes / non traduites, lui, n'avait jamais été fait, donc le nombre de
vraies traductions manquantes était inconnu.

À la remesure, il en restait **deux**, et une seule était une traduction anglaise manquante :

| Clé | Défaut | Correctif |
|---|---|---|
| `admin.sharedHint` | le catalogue **`fr`** disait « {{count}} users ({{pct}}) » | `fr` passé à « utilisateurs » |
| `org.pyramid.directCount_one` / `_other` | `en` rendait « 1 direct », « 5 direct » | « 1 direct report », « 5 direct reports » |

Les 92 autres sont légitimement identiques et **déclarées une fois pour toutes** dans
[`scripts/i18n-identical-allowlist.json`](../scripts/i18n-identical-allowlist.json), avec leur
catégorie et leur valeur française épinglée :

| Catégorie | Entrées | Exemples |
|---|---|---|
| `mot-identique` | 56 | Description, Total, Navigation, Collaboration |
| `anglicisme-fr` | 15 | Sprint, Blog, Drag & drop, Mini heatmap, Teal |
| `symbole-ou-format` | 10 | `{{count}} min`, `{{done}}/{{total}}`, `.` |
| `sigle` | 6 | OKR, FAQ, ID, OKR ID |
| `nom-propre` | 4 | Cosmo, thecosmo.app, Cosmo Premium |
| `identifiant-technique` | 1 | `cosmo-categories` (nom de fichier exporté) |

L'interface anglaise n'est donc **pas** une façade : elle est faite.

### La garde : `npm run i18n:identical`

Cliquet bloquant en CI, **à zéro**, sur les identités **non déclarées**. `-- --list` dit
lesquelles. Il ferme un angle mort structurel : le moteur retombe clé par clé sur `fr`, donc une
valeur jamais traduite s'affiche **en français dans une interface anglaise** et ne ressemble
jamais à une clé brute. `i18n:check` ne voit que les clés, jamais leur contenu.

L'allowlist est **vérifiée, pas crue**. Le script échoue si une entrée déclarée :

1. ne correspond plus à aucune clé, ou
2. a fini par être traduite (les valeurs diffèrent, la dispense n'a plus d'objet), ou
3. porte une valeur française différente de celle épinglée, ou
4. cite une catégorie qui n'existe pas.

Le point **3** est le plus important : sans la valeur épinglée, renommer « Manager » en
« Responsable d'équipe » ferait hériter la nouvelle chaîne de la dispense accordée à l'ancienne,
sans que personne ne la relise. C'est exactement la façon dont une garde cesse de mesurer sans
jamais cesser de répondre.

Témoin : [`scripts/i18n-identical.guard.test.mjs`](../scripts/i18n-identical.guard.test.mjs),
9 cas soumis au script réel dans un dossier temporaire. Vérifié en le sabotant de trois façons
(détecteur qui ne compte plus rien, contrôle de péremption neutralisé, allowlist qui dispense
tout) : 4, 4 et 2 cas tombent respectivement. Une garde dont on n'a jamais vu le témoin échouer ne
prouve rien.

- ❌ **Ne JAMAIS relever `MAX_UNDECLARED`** pour faire passer la CI. Une identité nouvelle est soit
  une traduction manquante, soit une légitime : dans les deux cas elle se traite, elle ne
  s'autorise pas en gros.
- ❌ **Ne jamais déclarer légitime une phrase.** Les six catégories couvrent des mots, des sigles,
  des formats et des noms. Une phrase entière identique dans les deux catalogues n'a qu'une
  explication : elle n'a pas été traduite.

## 2. 🟠 Mais le contenu éditorial est monolingue

> ✅ **Refermé le 2026-09-08.** Ce paragraphe décrivait `src/content/` comme dépourvu de
> dimension de locale : les 11 articles de blog et les 4 pages cas d'usage n'existaient qu'en
> français, soit 15 des 24 pages prérendues. **Ce n'est plus vrai** : les deux registres portent
> désormais un corps `en`, et c'est ce qui a permis la bascule ci-dessous.

### Conséquence : l'anglais est ouvert à l'indexation (C-20, 2026-09-08)

`INDEXABLE_LOCALES` vaut `['fr', 'en']`. La bascule n'a pas été décidée sur la config mais sur le
**contenu**, mesuré page par page avant d'y toucher. C'est la condition posée par
[`SEO.md`](./SEO.md) § « Ouvrir une langue à l'indexation » :

| Pages | Source du contenu | Corps anglais |
|---|---|---|
| Accueil, guide, à-propos, entreprise | `prerender.mjs` + catalogues `src/locales/` | ✅ 430 à 700 mots |
| 4 pages cas d'usage | `src/content/use-cases.mjs` | ✅ 579 à 815 mots |
| 11 articles de blog | `src/content/blog/*.mjs` | ✅ 657 à 1 985 mots |
| 3 pages contractuelles | `prerender.mjs` | résumé EN, `noindex` conservé (le français fait foi) |

Aucune page ne restait en français. Mesuré sur le `dist/` après build : **50 pages prérendues dont
26 en `/en`, 40 URLs au sitemap dont 20 en `/en`**, toutes avec `lastmod` et alternates
réciproques, et **zéro** URL déclarée au sitemap tout en étant `noindex`.

L'option « rendre `INDEXABLE_LOCALES` granulaire par route », envisagée ici, n'a **pas** été
retenue et n'était pas nécessaire : `availableLocales()` (prerender.mjs) fait déjà exactement
cette granularité, route par route, en n'publiant une locale que si elle a **méta ET contenu**.
C'est la garde structurelle qui rend impossible la publication d'une page anglaise au corps
français, et elle existait avant la bascule.

## 3. ✅ Ce que le socle garantit déjà

- **Parité des clés vérifiée en CI** (`i18n:check`, bloquant) : une locale ne peut plus partir en
  prod à moitié traduite sans que la CI le voie. C'est la garde qui manquait le plus.
- **Contenu des valeurs vérifié en CI** (`i18n:identical`, bloquant, cliquet à 0) : une valeur `en`
  restée en français ne peut plus passer en silence. `i18n:check` ne regarde que les clés.
- **Repli clé par clé sur `fr`** : une clé oubliée n'affiche jamais de clé brute à l'utilisateur.
- **Slugs de routes localisés** avec une URL canonique unique par langue.
- **`es` déclaré mais non servi** — présent dans `ALL_LOCALES` et `route-slugs.json`, absent de
  `SUPPORTED_LOCALES`. C'est le mécanisme prévu, pas une incohérence.

## 4. Règles

- ❌ **Ne jamais identifier une erreur par son message** — il est traduit. Utiliser un code.
- ❌ Ne jamais concaténer des fragments traduits : une clé = une phrase complète.
- ✅ Toute nouvelle chaîne visible passe par un catalogue. `npm run i18n:scan` détecte les oublis.
- ✅ Toute nouvelle clé se traduit **vraiment** dans `en`. `npm run i18n:identical` refuse une
  valeur recopiée du français qui n'est pas déclarée légitime.
- ✅ Avant d'ouvrir une langue à l'indexation, vérifier que **le contenu éditorial** est traduit,
  pas seulement l'interface — c'est la distinction que cet audit a mise au jour.

---

## Gardes i18n · repris de `CLAUDE.md` (déplacé le 2026-09-16)

> Commentaires de la section `## Scripts` de `CLAUDE.md`, où ils étaient chargés à chaque
> session. Déplacés ici **sans une coupe**. La racine ne garde que la commande.

```bash
npm run i18n:check          # Parité des clés fr ↔ en (CI, bloquant)
npm run i18n:scan           # Chaînes en dur non externalisées — **GATE CI depuis le 2026-09-02**
                            # Cliquet à **0** (`MAX_STRINGS`) : plus une chaîne détectée.
                            # `-- --list` dit LESQUELLES.
                            # 🔴 Le seuil a valu 4 pendant une journée, sur une heuristique
                            # aveugle à QUATRE formes entières : texte JSX contenant une
                            # interpolation, texte sur plusieurs lignes, propriété d'objet
                            # `label:` (vs attribut `label=`), et valeur par défaut de prop.
                            # Le rapport certifiait « plus aucune chaîne en dur » pendant
                            # qu'il en restait des dizaines dans le produit. Les quatre sont
                            # couvertes ; le chiffre était monté à 25 parce que la MESURE a
                            # changé, puis les 25 ont été extraites à leur tour.
                            # ❌ Ne jamais reprendre l'ancienne phrase « plus une seule chaîne
                            # d'interface en dur dans src/ » : elle a été VRAIE de la mesure et
                            # FAUSSE du produit pendant une journée. Le seul énoncé vérifiable
                            # est la sortie de `-- --list`, jamais un chiffre de tête.
                            # Deux exclusions ASSUMÉES : les commentaires de code (ce dépôt
                            # commente en français) et les seeds de démo, dont le français
                            # est la forme de référence recouverte par `localizeSeed` /
                            # `isEnglishSeed`.
                            # ❌ Ne JAMAIS relever le seuil pour faire passer la CI.
npm run i18n:identical      # Valeurs `en` IDENTIQUES au `fr` non declarees legitimes.
                            # GATE CI depuis le 2026-09-08, cliquet a **0**.
                            # `-- --list` dit LESQUELLES.
                            # Angle mort qu'il ferme : le moteur retombe cle par cle sur `fr`,
                            # donc une valeur jamais traduite s'affiche EN FRANCAIS dans une
                            # interface anglaise, sans jamais ressembler a une cle brute.
                            # `i18n:check` ne voit que les CLES, jamais leur contenu.
                            # 🔴 Le chiffre historique (« 71 valeurs, 3 % ») datait du
                            # 2026-08-14 et n'a jamais ete remesure pendant que le catalogue
                            # gagnait 1 403 cles. Remesure du 2026-09-08 : **92 / 3 808
                            # (2,4 %)**, dont **2 vraiment non traduites** ; les 90 autres
                            # etaient legitimes et n'avaient jamais ete triees.
                            # Les legitimes sont DECLAREES une fois pour toutes dans
                            # `scripts/i18n-identical-allowlist.json` (categorie + valeur `fr`
                            # EPINGLEE), sinon le prochain audit refait le meme tri a la main.
                            # L'allowlist est VERIFIEE : une entree dont la cle a disparu, qui
                            # a fini par etre traduite, dont la valeur `fr` a change, ou dont
                            # la categorie n'existe pas, fait ECHOUER la garde. Sans la valeur
                            # epinglee, renommer un libelle ferait heriter la nouvelle chaine
                            # de la dispense accordee a l'ancienne.
                            # ❌ Ne JAMAIS relever le seuil pour faire passer la CI.
                            # ❌ Ne jamais declarer legitime une PHRASE : les six categories
                            # couvrent des mots, des sigles, des formats et des noms.
                            # Temoin : `scripts/i18n-identical.guard.test.mjs` (9 cas), vu
                            # echouer sur trois sabotages du script avant d'etre committe.
npm run i18n:namespaces     # Quels catalogues le SHELL rend (donc eager) ; --pages
                            # donne la liste à déclarer par route dans App.tsx
npm run profile:landing     # Profil du fil principal d'une page (CPU bride, via Playwright).
                            # ⚠️ Mesure du 2026-08-30 : sur un poste de dev, la landing (55 en CI)
                            # et le guide (96 en CI) rendent le MEME score, la charge machine
                            # dominant tout. Un ecart entre deux PAGES ne se lit donc que sur le
                            # runner ; l'outil sert a comparer un AVANT/APRES sur la meme page.
```

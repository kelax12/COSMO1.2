# i18n — qualité des traductions et périmètre réel

**Audit du 2026-08-14** — premier audit de ce domaine. Mesuré sur les catalogues de
`src/locales/` et le contenu éditorial de `src/content/`.

Le fonctionnement du socle est décrit dans [`../CLAUDE.md`](../CLAUDE.md) §i18n ; l'indexation par
locale dans [`SEO.md`](./SEO.md). Ce document ne traite que d'une question : **le produit est-il
réellement bilingue ?**

---

## 1. Les catalogues d'interface sont réellement traduits

Méthode : comparer chaque valeur `en` à sa valeur `fr`. Une valeur identique signale une clé non
traduite (aux noms propres et symboles près).

| Mesure | Résultat |
|---|---|
| Clés dans le catalogue de référence `fr` | **2 476** sur 19 namespaces |
| Valeurs `en` identiques au `fr` | **71 (3 %)** |
| `npm run i18n:check` | ✅ 0 erreur, 0 avertissement — parité des clés respectée |

**3 %, c'est un très bon score**, et une partie de ces 71 valeurs est légitimement identique
(« Cosmo », « OK », « Notion », nombres, symboles). Les namespaces les plus concernés sont
`taskModal` (10 %), `eventModal` (9 %) et `admin` (6 %) — à repasser en priorité si on veut
descendre encore.

L'interface anglaise n'est donc **pas** une façade : elle est faite.

## 2. 🟠 Mais le contenu éditorial est monolingue

`src/content/` ne porte **aucune dimension de locale** :

- les **11 articles de blog** (`src/content/blog/*.mjs`) sont en français uniquement ;
- les **4 pages cas d'usage** (`src/content/use-cases.mjs`) aussi.

Or ces pages représentent **15 des 24 pages prérendues**. Un visiteur anglophone sur `/en/blog`
verrait donc l'interface en anglais et les articles en français.

### Conséquence : la règle d'indexation mérite d'être révisée

`src/i18n/seo-urls.mjs` justifie `INDEXABLE_LOCALES = ['fr']` par : « le **corps** des pages est
encore français, seules les méta sont traduites ». **Cette justification n'est plus exacte** — les
catalogues sont traduits à 97 %. Elle reste vraie pour le blog et les cas d'usage, faux pour le
reste.

Le périmètre se découpe donc en deux :

| Pages | Source du contenu | Traduites ? |
|---|---|---|
| Accueil, guide, à-propos, pages légales | catalogues `src/locales/` | ✅ oui |
| Blog (11 articles), cas d'usage (4 pages) | `src/content/*.mjs` | ❌ non |

**Deux chemins possibles**, à arbitrer :

1. **Ouvrir l'anglais à l'indexation sur le sous-ensemble traduit uniquement** (accueil + guide),
   en laissant blog et cas d'usage hors sitemap `/en/`. Gain SEO immédiat, coût nul, mais demande
   de rendre `INDEXABLE_LOCALES` granulaire par route au lieu d'être global.
2. **Traduire les 4 pages cas d'usage** (courtes, orientées conversion) pour ouvrir un périmètre
   anglais cohérent, en laissant le blog en français.

Ne rien faire est également défendable — mais alors il faut corriger le commentaire de
`seo-urls.mjs`, qui documente aujourd'hui une raison dépassée.

## 3. ✅ Ce que le socle garantit déjà

- **Parité des clés vérifiée en CI** (`i18n:check`, bloquant) : une locale ne peut plus partir en
  prod à moitié traduite sans que la CI le voie. C'est la garde qui manquait le plus.
- **Repli clé par clé sur `fr`** : une clé oubliée n'affiche jamais de clé brute à l'utilisateur.
- **Slugs de routes localisés** avec une URL canonique unique par langue.
- **`es` déclaré mais non servi** — présent dans `ALL_LOCALES` et `route-slugs.json`, absent de
  `SUPPORTED_LOCALES`. C'est le mécanisme prévu, pas une incohérence.

## 4. Règles

- ❌ **Ne jamais identifier une erreur par son message** — il est traduit. Utiliser un code.
- ❌ Ne jamais concaténer des fragments traduits : une clé = une phrase complète.
- ✅ Toute nouvelle chaîne visible passe par un catalogue. `npm run i18n:scan` détecte les oublis.
- ✅ Avant d'ouvrir une langue à l'indexation, vérifier que **le contenu éditorial** est traduit,
  pas seulement l'interface — c'est la distinction que cet audit a mise au jour.
